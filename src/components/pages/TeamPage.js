import React from 'react';
import TeamDetails from '../TeamDetails';

const TeamPage = () => {
    return (
        <div className="team-page container mx-auto p-4">
            <TeamDetails />
        </div>
    );
};

export default TeamPage;
